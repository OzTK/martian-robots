import "mocha";
import "should";

describe("mars-planner", () => {
    describe("run", () => {
        it("returns a list of robots with the same length as passed");
        it("returns the correct ending position and coordinate for each robot (Sample Input)");
    })
});